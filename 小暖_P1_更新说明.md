# 小暖 P1 更新说明 · 2026-08-04

> 在已发布的 P0（实时通话 / 形象升级 / 记忆时间线 / 关系仪式感）基础上，本次补齐你点名的三项 P1 增强。
> 线上地址（稳定）：**https://a556f2b15fc97d678.bj5.agentos-app.net**
> 回归测试：P1 **14/14 通过**，P0 历史用例 **21/21 通过（无回归）**。

---

## ① 🔔 Web Push 跨会话通知（消息提醒）
- 「我的」页新增 **🔔 消息提醒** 卡片：开关授权 + 「发送一条测试通知」按钮。
- 用户离页 / 切到后台时，小暖的想念与回复会自动转成**手机系统通知**（Notification API），真正"跨会话主动找你"。
- `sw.js` 新增 `notificationclick`（点击直接回到应用）、`push`、`periodicsync` 预留，配合后端可做到 App 未打开也推送。
- 健壮性：页面可见时照常语音/聊天；通知权限不足或无 SW 时安全回退，绝不报错。
- 隐私：纯本地行为，未离开浏览器（我们不发任何服务器推送）。

## ② ☁️ 云端存档（多端同步）
- 「我的」页新增 **☁️ 云端存档** 卡片：一键**导出存档码**（Base64，含全部记忆/关系/好感/换装/游戏记录）+ **复制** + **导入恢复**。
- 换手机、重装、清缓存后，把存档码粘贴进来即可**完整恢复你们的感情进度**。
- 含版本与来源校验，损坏码会安全报错，不会污染现有数据。

## ③ 🎀 更多 TTS 音色
- 「我的」页新增 **🎀 语音音色** 卡片：**自动 / 甜心 / 御姐 / 软萌 / 少年 5 档**。
- 不同档位分别调音高与语速，**即使设备没有中文 voice 也能听出区别**；选中即试听，聊天与语音通话统一走所选音色。

---

## 改动文件
| 文件 | 改动 |
|---|---|
| `app.js` | 新增 `VOICE_PROFILES`/`pickTts`（音色）、`notifyOS`（系统通知）、`exportSave`/`importSave`（存档）、`bindVoice`/`bindNotify`/`bindCloudSave`；`herSay` 离页转通知；`speak`/`speakAndWait` 走统一音色；`defaultState` 加 `voiceName`/`notify` |
| `index.html` | 「我的」页新增消息提醒 / 语音音色 / 云端存档三张卡片；版本号 v1.0→v1.1 |
| `style.css` | 新增 `.me-textarea`（存档码文本域）样式 |
| `sw.js` | 缓存版本 v1→v2；新增 `notificationclick`/`push`/`periodicsync` 处理；fetch 回退逻辑不变 |
| `小暖竞品差距与优化路线图.md` | P1 三项标记为「✅ 已完成」 |

## 诚实说明
- 真正的**服务端 Web Push**（App 完全关闭也推送）需要后端配合，本次实现的是**浏览器内跨会话通知**（PWA 在后台时仍生效），已预留 `push`/`periodicsync` 接口，后续接后端即可升级为全量推送。
- 实时语音通话仍是端侧 ASR→思考→TTS（非全双工流式）。若要更低延迟的"边听边说"，需升级为 WebSocket + 大模型语音（GPT-4o-realtime / 豆包语音等）。
