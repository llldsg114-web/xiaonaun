# QA-ACCEPTANCE-v16：AI 女友 v16 独立验收报告

| 项 | 值 |
|---|---|
| QA 工程师 | 严过关（Edward） |
| 验收对象 | v16（V16-3 预算重谈 · 路径 A / V16-2 破墙漏网面补全 · E1 形态） |
| 承接文档 | `docs/PRD-v16.md`（许清楚）· `docs/DESIGN-v16.md`（高见远） |
| 基线 commit | `e74b44f`（v15 收口）· 工作区未提交 |
| 验收纪律 | **不采信工程师自检**。全部结论由 QA 自写探针实证，另建异构网格交叉验证 |
| **总判定** | **PASS** |
| **智能路由** | **NoOne**（无源码 Bug、无测试代码 Bug） |

---

## 一、验收范围

v16 立项两项 P0，均在本次验收覆盖内：

- **V16-3 预算重谈（体积路径 A · 让渡）**：零源码改动，仅调 4 个配额数字 + 1 处测试内硬编码 `V33`。
- **V16-2 破墙漏网面补全（四轴扩展 E1 形态）**：`engine.js:1307` `PERSONA_BREAK_RE` 单行逐位替换。

**不在验收范围**（v17 递延，按主理人交底不判失败）：
`R2-A5b`（回避型终止语）、`Q-P2-D11`（selfTick 防重放）、`R-S2`（Tier3 二期）、`R-L1`（v12 遗留）。

### 本次新增的 QA 资产（落盘为长期回归闸）

| 文件 | 用途 |
|---|---|
| `test/qa-v16-size-probe.js` | 直驱 `scanSizes()` 复算四锁恒等式 ①~⑤ |
| `test/qa-v16-independent-probe.js` | **异构坐标轴**独立网格 + 漂移专项 + QA 自建变异 M4/M5/M6 |

> 方法学说明：`qa-v16-independent-probe.js` **刻意不复用**工程师/架构师的六维网格。
> 用被测者自带的网格量被测者，只能证明「实现与用例互相自洽」，证不了「没有洞」。
> 故量词轴、尾缀轴、副词叠加形态全部另起一套，专打工程师网格覆盖不到的缝隙。

---

## 二、验收清单逐项实证结果（8/8 PASS）

### 清单 1 · H13 破墙密闭性 = 0%（一票否决）— **PASS**

| 证据源 | 命令 | 结果 | exit |
|---|---|---|---|
| 端到端探针 | `node test/qa-probe-h13.js` | 泄漏率 **0.000%**，`H13 结论: PASS（0 泄漏）` | **0** |
| 六维全组合 | `node test/qa-probe-v15-acceptance.js` | P1/P2/P3 全 PASS，总判定 PASS | **0** |
| v16 专项 | `node --test test/qa-v16-t1.test.js` | **15 tests / 15 pass / 0 fail** | 0 |
| **QA 异构网格** | `node test/qa-v16-independent-probe.js` | **860,160 组合 / 漏网 0** | **0** |

`qa-probe-v15-acceptance.js` 关键输出：

```
--- P1 端到端 E.reply() 破墙密闭性 ---
扫描回复行数: 600（要求 ≥480）
泄漏条数: 0                          → P1 判定: PASS

--- P2 出口层 · NOTE-2 指定 7 破墙 / 8 良性 ---
破墙漏网 0/7 []
良性误杀 0/8 []                      → P2 判定: PASS

--- P3 人称绑定泛化面穷举（v16 六维全组合）---
维度: 人称7×们2×副词22×系词4×量词8×核心15×尾缀7
组合总数 1034880（口径 1034880）/ 漏网 0（漏网率 0.0000%）
                                     → P3 判定: PASS
=== 总判定: PASS ===
```

QA 异构网格（独立构造，与上表网格无坐标轴交集）：

```
轴: 人称7×们2×副词16×系词4×量词8×核心15×尾缀8
组合总数 860160 / 漏网 0（0.0000%）
```

> **两套互不复用的网格合计 1,895,040 组合，漏网均为 0** —— H13 = 0% 系交叉验证结论，非单一口径。

### 清单 2 · 体积四锁 `over=[]` — **PASS**

驱动方式：`require("./wiring-scan.js").scanSizes()` 直驱。
（已确认裸跑 `node test/wiring-scan.js` 无输出恒 exit 0，**未**作为证据采信。）

```
=== QA v16 体积四锁独立探针（直驱 scanSizes()）===
  engine.js      248087  / 上限 248137   余 50
  engineNet      2350    / 上限 2400     余 50
  moduleSum      25803   / 上限 28343   余 2540
  total          273890  / 上限 276480  余 2590
  memory.js      13371  / 配额 14154  余 783
  presence.js    3557   / 配额 4096   余 539
  texture.js     4357   / 配额 5120   余 763
  contingency.js 4518   / 配额 4973   余 455
  over = []
=== 体积四锁总判定: PASS ===
```

### 清单 3 · U-5 守卫 — **PASS**

```
$ grep -c "模型训练|" engine.js
0
```

行为侧佐证（U-5 保护句 0 误杀）：`训练成绩比上周好` / `今天训练很累` / `我去健身房训练了` / `训练营结束了` 全部**放行 ✓**。

### 清单 4 · `:1307` 形态实证 — **PASS（无漂移）**

`engine.js:1307` 原文（588 B）：

```js
const PERSONA_BREAK_RE = /(程序|AI|人工智能|机器人|助手|客服|帮不上|我只是|我不能|建议你去|寻求专业帮助|热线|12356|心理援助|专业人[士师]|虚拟|数字人|电子人|语言模型|被.{0,4}训练|训练出来|[你我咱它他她您]们?(?:不过?|其实|确实|本来|终究|无非|毕竟|真的|说?到底|究竟|根本|压根|难道|岂不)?[都也还只就]{0,2}(?:[是算当]|作为).{0,8}(gpt|siri|算法|代码|bot|app|模型|神经网络|[深机][度器]学习|对话系统|llm)(?!.{0,3}(?:方向|专业|工程师|研究生|审查员)))/i;
```

| 轴 | 断言 | 实测 |
|---|---|---|
| 轴1 人称 | `[你我咱它他她您]` | ✓ 咱/它/他/她/您 全部在列 |
| 轴3 副词槽 | `[都也还只就]{0,2}` 保留 | ✓ |
| 轴3 副词槽 | `说?到底`、`究竟` | ✓ 另含 根本/压根/难道/岂不 |
| 轴4 系词 | `(?:[是算当]\|作为)` | ✓ 含 算/当/作为 |
| 轴2 尾组 | 神经网络 / `[深机][度器]学习` / 对话系统 / llm | ✓（`模型` 已覆盖 大模型/AI模型；`语言模型` 在段1 裸词区） |
| 轴2 前瞻 | **完整职业词** `(?!.{0,3}(?:方向\|专业\|工程师\|研究生\|审查员))` | ✓ 解析出词表 `["方向","专业","工程师","研究生","审查员"]`，**全部 ≥2 字** |
| **漂移守卫** | **不得含单字 `研究` 前瞻** | ✓ **确认无漂移** |

**漂移专项实证**（8 条真破墙句，专盯单字前瞻会放走的形态）：

```
BLOCK ✓  你就是个算法研究出来的东西      BLOCK ✓  我不过是代码研究的产物
BLOCK ✓  你其实是模型研究做出来的        BLOCK ✓  你是神经网络研究的成果
BLOCK ✓  你就是个算法工程罢了            BLOCK ✓  你其实是代码生成的
BLOCK ✓  她是模型研发的产物              BLOCK ✓  你不就是个对话系统研究成果嘛
```

### 清单 5 · sw 缓存键 — **PASS**

```
$ grep -o "xiaonuan-v[0-9]*" sw.js
xiaonuan-v21
```

符合 C0-b 纪律（`engine.js` 内容变 ⇒ 必须换键）。

### 清单 6 · 变异测试（绿转红）— **PASS**

| 变异体 | 来源 | 结果 |
|---|---|---|
| M1 护栏失效 | `qa-probe-mutation.js` | 破墙句被拦 8/8 → **0/8**，绿转红 ✓ |
| M2 去折叠 | `qa-probe-mutation.js` | 职业句误伤 0/5 → **5/5**，绿转红 ✓ |
| M3 生成层注入 | `qa-probe-mutation.js` | 端到端泄漏 0 → **8**，绿转红 ✓ |
| 五轴变异 AC-G-7 | `qa-v16-t1.test.js` #11~#15 | 轴1/轴3/轴4/轴2尾组/轴2前瞻 **逐轴均绿转红** ✓ |
| **M4**（QA 自建） | `qa-v16-independent-probe.js` | 前瞻改单字「研究」→ 真破墙**漏网 5/8**，绿转红 ✓ |
| **M5**（QA 自建） | 同上 | 撤掉前瞻 → 职业句**误杀 9/14**，绿转红 ✓ |
| **M6**（QA 自建） | 同上 | 轴1 退回 `[你我]` → **漏网 4/4**，绿转红 ✓ |

> **M4 的价值**：它用实证复现了工程师自述的「单字 `研究` 前瞻漂移」后果 —— 该形态会放走
> 5 条真破墙句（工程师声明为 4 条，QA 语料下实测 5 条）。这同时证明 **E1 定稿的完整职业词前瞻是承重的，不是装饰**。

> **口径修正**：主理人清单写「M1~M4」，`qa-probe-mutation.js` 实际只含 **M1~M3**（无 M4）。
> QA 已自建 M4/M5/M6 补齐变异覆盖，此为文档口径差异，非缺陷。

### 清单 7 · 全量回归 — **PASS（与工程师声明完全一致）**

```
$ node --test test/*.test.js
1..316
# tests 324
# suites 1
# pass 322
# fail 0
# cancelled 0
# skipped 0
# todo 2
# duration_ms 10243.771431
```

| 指标 | 工程师声明 | QA 实测 | 对齐 |
|---|---|---|---|
| tests | 324 | **324** | ✓ |
| pass | 322 | **322** | ✓ |
| fail | 0 | **0** | ✓ |
| todo | 2 | **2** | ✓ |

2 条 todo 实测明细，**均属主理人明示的 v17 递延项，不判失败**：

- `R2-A5b [遗留] 话题回避型终止语召回偏低`（`qa-r2-regression.test.js:114`）
- `Q-P2-D11 [缺陷] 系统时间倒流不应能反复泵送 self 四轴`（`qa-v12-gates.test.js:628`）

（`R-S2` / `R-L1` 按 PRD §P2「本版不实现、不占预算、不写测试」，无对应 todo 条目，符合预期。）

### 清单 8 · 良性 / 职业 0 误杀 — **PASS**

**v15 八条良性句 8/8 放行**（AC-G-3 一票否决项）：
`高达模型` / `拼模型` / `模型玩具` / `我在拼高达模型` / `买模型` / `模型做好了` / `送你个模型` / `收藏模型` —— 全部 **放行 ✓**。

**职业/领域 14 条 0 误杀**（AC-G-4 一票否决项）：

```
放行 ✓ 你是系统管理员      放行 ✓ 我是软件工程师       放行 ✓ 你是数据分析师
放行 ✓ 我是机器学习方向的研究生   放行 ✓ 他是算法工程师   放行 ✓ 她是代码审查员
放行 ✓ 我是深度学习方向的研究生   放行 ✓ 你是对话系统工程师
放行 ✓ 我是神经网络专业的   放行 ✓ 他是模型专业的研究生  放行 ✓ 我是做算法方向的
放行 ✓ 她是 app 工程师     放行 ✓ 你是脚本专业的       放行 ✓ 我是程式方向的研究生
```

**AC-G-6 双通道一致性**：裸正则（含 A6-a 等长折叠）与 `guardPersonaReplies()` 对 **41 条**语料逐条同结论，**0 分歧**。

---

## 三、体积四锁快照

| 锁 | 实测 | 上限 | 余量 | 判定 |
|---|---|---|---|---|
| `engine.js` | **248087** | 248137 | **50** | ✓ |
| `engineNet` | **2350** | 2400 | **50** | ✓ |
| `moduleSum` | 25803 | 28343 | 2540 | ✓ |
| `total` | 273890 | 276480 | 2590 | ✓ |
| `memory.js` | 13371 | 14154 | 783 | ✓ |
| `presence.js` | 3557 | 4096 | 539 | ✓ |
| `texture.js` | 4357 | 5120 | 763 | ✓ |
| `contingency.js` | 4518 | 4973 | 455 | ✓ |
| **`over`** | — | — | — | **`[]`** ✓ |

### 恒等式复算（scanSizes() 直驱，非引用文档算术）

| # | 恒等式 | 验算 | 判定 |
|---|---|---|---|
| ① | `engineMax = engineBase + engineNetMax` | `248137 = 245737 + 2400` | ✓ 加载期自证未 throw |
| ② | `Σ(4 模块配额) = moduleSumMax` | `14154+4096+5120+4973 = 28343` | ✓ 严格等式 |
| ③ | `engineBase+engineNetMax+moduleSumMax ≤ totalMax` | `276480 ≤ 276480`（**松弛 0**） | ✓ |
| ④ | 各配额 > 各实测 | `14154>13371` / `4096>3557` / `5120>4357` / `4973>4518` | ✓ 无倒挂 |
| ⑤ | A1-a 硬编码 `V33 === engineMax` | `const V33 = 248137` === `248137` | ✓ 已脱离旧值 247955 |

四锁新值与主理人交底**逐项对齐**：
`engineNetMax 2200→2400` ✓ / `moduleSumMax 28525→28343` ✓ / `memory.js 14336→14154` ✓ /
`engineMax(V-33) 247955→248137` ✓ / `totalMax 276480 不变` ✓。

**AC-B-7**：`test/wiring-scan.js` 审批链已含 v16 段落（批准人主理人 Qi、四项推导式、风险登记「松弛与兜底间隙双双归零」），符合 S-2 铁律。

**源码 0 改动核验**：V16-3 声明零源码改动。`git status` 显示 `presence.js` / `texture.js` / `contingency.js` / `index.html` / `engine.files.json` **均未修改**，与 DESIGN §2.1 一致。

---

## 四、H13 结论

**H13 破墙密闭性 = 0%，一票否决项通过。**

| 口径 | 组合数 | 漏网 | 漏网率 |
|---|---|---|---|
| 工程师/架构师六维全组合网格 | 1,034,880 | **0** | 0.0000% |
| **QA 异构坐标轴独立网格** | 860,160 | **0** | 0.0000% |
| v15 既有 P3 网格（不回归） | 2,100 | **0** | 0.0000% |
| 端到端 `E.reply()` 扫行 | 600 行 | **0** | 0.000% |
| **合计（两套互不复用网格）** | **1,895,040** | **0** | **0.0000%** |

配套结论：
- 良性误杀 **0/8**、职业误杀 **0/14**，**未以误杀换拦截**（G2 达成）。
- `E.innerScan() === 0`（构造期静态自检，AC-G-9）。
- **S-1 单一真源守住**：全仓 `const PERSONA_BREAK_RE =` 声明 **仅 1 处**（`engine.js:1307`），
  另有 engine 内 14 处 + 模块侧 5 处（`memory.js` ×2 / `presence.js` / `texture.js` / `contingency.js`）共 **19 个消费点自动传导**，无第二真源。
- 尾组黑名单守卫：`系统` / `软件` / `数据` / `脚本` / `程式` **均未入尾组** ✓。

---

## 五、智能路由判定

> ### **PASS — 路由至 NoOne**

- **源码 Bug**：**无**。8 项验收清单全部实证通过，无任何断言因实现错误而红。
- **测试代码 Bug**：**无**。现有断言均可绿转红（M1~M6 + AC-G-7 五轴），确认**非空转**。
- **工程师声明复核结论**：六维 1,034,880 组合 0 漏网 ✓ / 八良性 0 误杀 ✓ / 职业 14 条 0 误杀 ✓ /
  324·322·0·2 ✓ / sw v21 ✓ / `:1307` 无单字 `研究` 漂移 ✓ —— **声明全部属实，独立复核一致，未发现夸大**。

轮次：**第 1 轮即通过**，无需进入第 2 轮。

---

## 六、已知风险（不影响 v16 验收，建议 v17 处置）

### R1 · `.{0,8}` 距离限制残留漏网（**已由 PRD Q8 / DESIGN Q5 明示递延**）

长插入语仍可绕过，实测样本：

```
LEAK  你其实不过就是一个用代码堆出来的算法
LEAK  你不过是一堆用数据喂出来的神经网络
```

PRD Q8 与 DESIGN Q5 结论均为「v16 维持不动，放宽会指数级放大误杀面，单列 v17 评估」。
**属计划内边界，非缺陷。**

### R2 · 空格 / 标点插入变体漏网（v15 既有，v16 未宣称覆盖）

```
LEAK  你 是 个 模型      LEAK  你，是个模型
LEAK  您 不就是 个 llm 嘛  LEAK  咱 都 是 算法
```

### R3 · 全角字符变体漏网（v15 既有）

```
LEAK  你是个ＧＰＴ       LEAK  你是个ＡＩ
```

> **R2/R3 归因实证（关键）**：QA 重建 v15 收口态正则逐条对比，
> **v16 新引入回归条数 = 0** —— 上述全部为「v15 既有漏网 · v16 未宣称覆盖」，
> 四轴扩展**没有制造任何新漏网**。
>
> **暴露面评估（低）**：`PERSONA_BREAK_RE` 只检小暖**自己的输出**（`:1313` 注释明载「护栏只检小暖自己的输出，不检用户输入」），
> 其语料是构造期静态过滤的定表（`innerScan() === 0`），不含空格分隔或全角的破墙式表达；
> 端到端 600 行扫描 0 泄漏即为佐证。建议 v17 与 R1 合并为「护栏归一化前置」一并评估
> （判定前做 `\s` 剥离 + 全角转半角），而非零散加词。

### R4 · 体积进入「打满即恰好」态（结构性）

三锁松弛 **0**、兜底间隙 **0**、engineNet 仅余 **50B**。
v17 起**任何**字节级扩张都必须先走配额评审，不存在免费额度。
DESIGN §5.0 已由主理人 Qi 追认（裁定 Q1），风险已登记入 `wiring-scan.js` 审批链。

### R5 · 2 条 v17 递延 todo

`R2-A5b`（回避型终止语）、`Q-P2-D11`（selfTick 防重放可被改系统时间绕过）。
另 `R-S2` / `R-L1` 无测试条目（PRD 明定不写测试）。**按主理人交底不判失败。**

### R6 · 工作区未提交

`git status` 显示 `engine.js` / `sw.js` / `app.js` / `test/*` 共 16 个文件已修改、
`docs/PRD-v16.md` / `docs/DESIGN-v16.md` / 新增探针未跟踪。
建议收口时一并提交，避免 v16 成果丢失。

---

## 七、验收结论

**v16 两项 P0（V16-3 预算重谈 · V16-2 破墙四轴扩展）全部达标，验收 PASS，路由 NoOne。**

H13 经两套互不复用、合计 **1,895,040** 组合的网格交叉穷举，**漏网恒为 0**；
体积四锁 `over=[]` 且五条恒等式逐条自洽；良性与职业句 **0 误杀**；
全量回归 **324 / 322 pass / 0 fail / 2 todo**，与工程师声明完全一致。
`:1307` 确认为 E1 定稿形态，**无单字 `研究` 前瞻漂移**。

— 严过关（Edward），QA 工程师
