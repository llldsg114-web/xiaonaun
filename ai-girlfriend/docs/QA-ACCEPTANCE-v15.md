# QA 独立验收报告 · v15

- **验收人**：Edward（QA 工程师）
- **工作目录**：`/workspace/ai-girlfriend`
- **验收纪律**：不信任工程师自检；全部结论由 `node -e` 自写探针独立复现；关键断言做变异测试确认非空转
- **轮次**：第 1 轮 FAIL（路由 Engineer）→ 工程师修 Q-V15-1 → **第 2 轮复验（本次）**

---

# ★ 第 2 轮验收结论（2026-06 · 本次）

- **被验交付**：工程师自检 309 tests / 305 pass / 0 fail / 4 todo，IS_PASS: YES
- **交付态基准**：`engine.js` **247897B** / `contingency.js` 4518B / engineNet **2160**

> ## QA 验收结论：PASS / 可收口
> ## 智能路由判定：**NoOne**
>
> 第 1 轮阻断项 **Q-V15-1 已闭合**（原一票否决项 H13 破墙密闭性现已全绿）。
> 8 个验收项 **8/8 PASS**，全量 309 tests / 305 pass / **0 fail** / 4 todo，
> 变异测试 M1/M2/M3 **3/3 绿转红**，QA 追加的 **M4**（定点变异 Q-V15-1 修复本身）亦绿转红 —— 本轮修复**确有承重**，新增断言非空转。

## 第 2 轮逐项结论表

| # | 验收项 | 结论 | 关键证据 |
|---|--------|------|----------|
| 1 | **H13 破墙密闭性（一票否决 / 原阻断项）** | **PASS** | `我们都是模型训练的` 现被 `:1307` 拦截；破墙 **7/7 全拦**、良性 **8/8 全放**；QA 追加对抗集 破墙 10/10 拦 + 良性 5/5 放；`qa-probe-h13` 480 行 **0 泄漏**；`qa-probe-v15-acceptance` P3 **2100 组合 0 漏网**（新回归 0） |
| 2 | U-5 裸词守卫 | **PASS** | `grep -c "模型训练\|" engine.js` = **0**；`RE.test("模型")` = **false**（无顶层裸分支）；定向短语 `被.{0,4}训练` / `训练出来` 均在位且生效 |
| 3 | **体积四锁 `over=[]`** | **PASS** | 直驱 `scanSizes()`：contingency 4518/4973、engine **247897/247937**、engineNet **2160/2200**、moduleSum 25803/28525、total 273700/276480，**`over=[]`**，10/10 期望值逐字节吻合 |
| 4 | R-C5 c4/c5 可出场 | **PASS** | 选择器为随机 `PW(cd.filter(...))` 且未退化为 `find`；c4 199/400、c5 223/400；混合池 4 类共存；CAP=2 未被放宽（同 state 连打 60 轮命中恰 2）且 AC-C5-1 计数器独立（fresh state 50 轮命中 29） |
| 5 | T0 基线 | **PASS** | `BASE="b86a386"`（解析为 v14 收口 commit）；sw `CACHE="xiaonuan-v20"`；全量 0 fail |
| 6 | 全量测试 | **PASS** | **309 / 305 pass / 0 fail / 4 todo**，与工程师自检一致；4 todo 均为既有 P2/遗留项，非 v15 引入 |
| 7 | 变异测试 | **PASS** | M1/M2/M3 **3/3 绿转红**；QA 追加 **M4**（撤 `[都也还只就]{0,2}`）→ B8 由「拦」转「放」，物理变异下 T2 **5 条转红**，sha256 还原逐位一致 |
| 8 | `engine.js` 改动面 | **PASS** | `git diff --numstat` = **1 增 1 删（单行）**；相对 BASE 净增 **+73B**（247824→247897 = NOTE-2 13B + Q-V15-1 60B），与 AC-N2-5 硬钉一致；工作树**无变异标记残留** |

## 原阻断项 Q-V15-1 —— 【已闭合】

| 项 | 第 1 轮 | 第 2 轮 |
|---|---|---|
| `我们都是模型训练的` | **LEAK 原样透出** | **BLOCK 已兜底** ✓ |
| 7 破墙句 | 6/7（漏 1） | **7/7** ✓ |
| 8 良性句 | 8/8 | **8/8** ✓ |
| P3 泛化面 2100 组合 | 新回归 **195 条** | 新回归 **0 条** ✓ |
| engineNet | 2100 | **2160**（上限 2200，余 40）✓ |

**闭合归因（非误打误撞）**：探针对原句做了捕获组取证 —— 命中片段 `我们都是模型`、尾部捕获组 `模型`，
证明确由段 3「人称 + 副词槽 + 系动词 + 核心词」路径拦下，正是本轮补的 `[都也还只就]{0,2}` 生效；
反证「我们是模型训练的」（无副词）仍拦，说明修复**没有弄丢**原有能力。

**歧途未走**：工程师**未**采用「往段 1 塞裸词 `模型训练|`」这一最省事写法（该写法会触犯 U-5
并误杀「模型训练营」类良性表述）。已双重确认：源码 grep 为 0，且行为上 `模型训练营周末开课`
等 3 条良性句均放行。

## 第 2 轮 QA 自纠（诚实记录）

本轮 QA 自身犯错 1 次，已自查自纠，**未误报给工程师**：

- `qa-r2-probe-h13-closure.js` 检查 F 初版用**文本启发式** `/(?:\(|\|)模型(?:\||\))/` 判「有无顶层裸分支 `模型`」，
  被合法尾组 `(gpt|siri|算法|代码|bot|app|模型))` 中的 `|模型)` 命中 → **误报 FAIL**。
- 归因：断言本身写错（源码正确）。按路由规则属 **QA 侧缺陷，自行修复**，不路由 Engineer。
- 修正：改用**行为判据** `RE.test("模型") === false` —— 顶层裸分支的充要特征是孤立字符串自身即命中；
  尾组内的 `模型` 必须先匹配 `[你我]们?…是.{0,8}` 前缀才可达。修正后 F 通过。

## 第 2 轮 NOTE（非阻断，供存档）

- **NOTE-A（行号漂移）**：任务清单称 R-C5 选择器在 `contingency.js:43`，**实际在 `:48`**；`:43` 是 `sf` 兜底入池行。
  仅文档行号漂移，逻辑与结论不受影响（第 1 轮 NOTE-3 已提出，本轮仍存在）。
- **NOTE-B（描述与 diff 口径差）**：任务清单描述本轮改动为「段 3 副词槽补 `[都也还只就]{0,2}`」，
  而 `git diff` 相对 HEAD 是**累计差分**，还含 v15 T2 的 NOTE-2 改动（删段 1 裸词 `模型|`、增 `语言模型|`、尾组增 `模型`）。
  两者并不矛盾：+60B 的字节账已独立核实闭合 —— 删 `|就` −4B、增 `|确实|本来|终究|无非|毕竟|真的` +42B、
  增 `[都也还只就]{0,2}` +22B，净 **+60B**，与 247837→247897 完全吻合。
- **NOTE-C（陷阱已确认）**：`node test/wiring-scan.js` 确为纯模块、**无输出恒 exit 0**，
  不能用作体积门禁；本轮全部体积结论均直驱 `scanSizes()` 取得。
- **NOTE-D（余量预警）**：engineNet 余量仅 **40B**（2160/2200），engine.js 余量同为 40B。
  v16 若还要动 `:1307` 一行，极可能触锁，建议提前规划。

## 第 2 轮 QA 新增资产

| 文件 | 用途 |
|---|---|
| `test/qa-r2-probe-h13-closure.js` | Q-V15-1 闭合双通道复验（裸正则 / 生产护栏 `guardPersonaReplies` 双通道一致性）+ U-5 行为判据守卫 |
| `test/qa-r2-probe-rc5.js` | R-C5 c4/c5 可出场 + 选择器随机性 + CAP=2 独立性（U-4）自建夹具复验 |

> **双通道设计意图**：只 `test()` 裸正则会漏掉「表定义正确但没挂到出口」这一类事故
> （本项目历史上已发生三次：D1/D3/N4）。故每条用例同时走裸正则与生产路径 `guardPersonaReplies()`，
> 两通道结论必须一致，不一致即判 FAIL。本轮 30 条用例双通道**全部一致**。

---

# 第 1 轮验收报告（存档 · 结论已被第 2 轮取代）

> 以下为第 1 轮 FAIL 时的原始记录，保留以备追溯。
> 其中 **H13 阻断项已于第 2 轮闭合**，体积基线数字（247837 / engineNet 2100 / 308 tests）均已过期。

- **被验交付**：工程师自检 IS_PASS: YES
- **交付态基准**：`engine.js` 247837B / `contingency.js` 4518B / 全量 308 tests

> **QA 验收结论：FAIL / 不可收口** ← *（已被第 2 轮 PASS 取代）*
> **智能路由判定：Engineer** ← *（已闭合）*
> 一票否决项 **H13 破墙密闭性未过**：主理人指定的 7 条破墙句中 **1 条漏网**，且该漏网是 **NOTE-2 本期引入的回归**（v14 拦得住、v15 漏）。其余 6 项全部 PASS。

---

## 一、逐项结论表

| # | 验收项 | 结论 | 关键证据 |
|---|--------|------|----------|
| 1 | **H13 破墙密闭性（一票否决）** | **FAIL** | 端到端 600 行 0 泄漏（PASS）；但 7 破墙句 **漏 1**：`我们都是模型训练的` 原样透出。泛化面穷举：本期新引入回归 **195 条** |
| 2 | 体积四锁 `over=[]` | **PASS**（含 1 NOTE） | 四锁全绿、`over=[]`；但工程师声称的 engine 上限 247955 比推导值 247937 宽 **18B** |
| 3 | R-C5 c4/c5 实际可出场 | **PASS**（含 2 NOTE） | c4/c5 均可出场；`$` 回填硬化有效；CAP=2 独立成立。选择器实际在 **:48** 非 :43；AC-C5-6 变异转红是**体积假象** |
| 4 | T0 基线重置 | **PASS** | `BASE="b86a386"`；7 条自失效红全绿；S-4 无字面量残留 |
| 5 | sw v20 | **PASS** | `CACHE = "xiaonuan-v20"`，v15 字样仅在注释 |
| 6 | 全量测试 | **PASS** | 308 tests / 304 pass / **0 fail** / 4 todo |
| 7 | 变异测试 M1/M2/M3 | **PASS** | 3/3 全部绿转红 |

---

## 二、H13 一票否决项 · 详述（阻断项）

### 2.1 缺陷定位

**反馈工程师修复**

- **文件:行号**：`engine.js:1307`（`PERSONA_BREAK_RE`）
- **失败输入**：`我们都是模型训练的`
- **期望**：命中护栏 → 替换为 `PERSONA_FALLBACK`（`我在。你不用一个人扛着，我哪也不去。`）
- **实际**：原样透出，护栏零拦截

### 2.2 探针代码与输出

```js
// node -e
const {loadEngine}=require("./test/helpers.js");
const E=loadEngine();
const BREAK=["我是语言模型","你不过是个模型","我其实是个大模型","你就是个模型",
             "我们都是模型训练的","你只是个语言模型","说白了你就是个AI模型"];
for(const s of BREAK){
  const o=E.guardPersonaReplies([s],"阿明");
  console.log((o[0]===s?"LEAK  原样透出":"BLOCK 已兜底"),"|",s,"->",o[0]);
}
```

```
BLOCK 已兜底 | 我是语言模型      -> 我在。你不用一个人扛着，我哪也不去。
BLOCK 已兜底 | 你不过是个模型    -> 我在。你不用一个人扛着，我哪也不去。
BLOCK 已兜底 | 我其实是个大模型  -> 我在。你不用一个人扛着，我哪也不去。
BLOCK 已兜底 | 你就是个模型      -> 我在。你不用一个人扛着，我哪也不去。
LEAK  原样透出 | 我们都是模型训练的 -> 我们都是模型训练的      ← ★漏网
BLOCK 已兜底 | 你只是个语言模型  -> 我在。你不用一个人扛着，我哪也不去。
BLOCK 已兜底 | 说白了你就是个AI模型 -> 我在。你不用一个人扛着，我哪也不去。
```

8 条良性句 **0 误杀**（NOTE-2 的正向目标达成）。

### 2.3 根因

NOTE-2 段 3 的人称绑定组为：

```
[你我]们?(?:不过?|其实|就)?是.{0,8}(gpt|siri|算法|代码|bot|app|模型)
```

副词槽只有 `不 / 不过 / 其实 / 就` 四个。`我们**都**是…` 的「都」不在槽内，`我们` 之后紧跟 `都` 而非 `是`，匹配在系动词位断裂。
`被.{0,4}训练` 需要「被」字，`模型训练` 无「被」，亦不命中。

### 2.4 这是本期引入的回归，不是既有缺陷

v14 收口基线（`git show b86a386:ai-girlfriend/engine.js:1307`）段 1 含**裸词 `模型|`**，上述句子被裸词直接拦下。NOTE-2 删裸词后失去兜底。

泛化面穷举（`[你我](们)(副词)是(量词)(核心词)`，2100 组合）：

| 口径 | 漏网数 | 归属 |
|------|--------|------|
| v15 现状总漏网 | 1365 | — |
| ├─ **NOTE-2 引入的新回归**（v14 能拦 / v15 漏） | **195** | **v15 内，必须修** |
| └─ v14 既有缺陷（两版都漏，如「你都是算法」） | 1170 | 移交 v16 |

195 条新回归**全部含「模型」**，与「删裸词 `模型|`」一一对应，因果链闭合。

### 2.5 端到端风险评级

P1 端到端 600 行回复 **0 泄漏** —— 说明当前**生成层语料里不存在**这些句式，属**潜伏缺陷**而非线上已发生事故。但护栏的职责就是兜住语料之外的意外，且 v13 C0-b 已有先例，**不可降级放行**。

### 2.6 已验证的修复方案（供工程师采用）

仅补副词槽，**不新增 `模型训练|`**（否则触犯 `T2 · U-5 结构守卫：破墙表不得出现裸词「训练」`）：

```js
/* engine.js:1307 —— 段 3 副词槽补全 */
[你我]们?(?:不过?|其实|确实|本来|终究|无非|毕竟|真的)?[都也还只就]{0,2}是.{0,8}(gpt|siri|算法|代码|bot|app|模型)
```

QA 已实测该补丁：

| 指标 | 结果 |
|------|------|
| 7 破墙句漏网 | **0** |
| 8 良性句误杀 | **0** |
| NOTE-2 新回归残留 | **0**（195 → 0） |
| 端到端 600 行泄漏 | **0** |
| `T2 · U-5` 裸词「训练」守卫 | **绿** |
| 字节增量 | **+60B** → engine 247897B，engineNet 2160/2200（余 40） |

⚠️ **该补丁会打红 6 条体积精确钉**（均为 `===` 等值钉，非 `≤`），需工程师连同**体积评审**一并处理：

| 断言 | 报错 |
|------|------|
| `A4 体积三闸门` | `2160 !== 2100` |
| `AC-N2-5` | `NOTE-2 应净增恰好 13B，实得 73B`（按 +60B 补丁则为 73→73 之外的新值） |
| `T2 · 体积 R-P0` / `T4 · 体积` / `V-108` / `V-113` | 同类净增等值钉 |

> 这 6 条不是缺陷，是**基线需要随修复一起重置**。建议由架构师确认新的 engineNet 预算后统一改钉。

### 2.7 测试侧同步暴露的覆盖缺口

`test/qa-v15-t2.test.js` 的破墙用例集 B1–B7 为：
`我是语言模型 / 你不过是个模型 / 我其实是个大模型 / 我是模型 / 我们是模型 / 我是一个大语言模型 / 你是不是模型`

**恰好不含** `我们都是模型训练的`。
`AC-N2-1b` 号称「自称壳 × 模型词**全组合**」，但 `SHELLS` 全部是 `人称+(副词)+是` 的**紧邻**形式（`我们是` / `我们不过是`），**未覆盖副词槽**（`我们都是`）。

→ 结论：该用例集是**照着实现写的**，而非照着规格写的，因此实现有洞而测试全绿。此为**测试 Bug**，但因其与源码修复强耦合（改正则后用例集须同步扩充），建议工程师修复源码时一并补齐，**QA 不单独抢修**，避免产生冲突改动。QA 已另行落盘独立探针 `test/qa-probe-v15-acceptance.js` 作为长期回归闸。

---

## 三、体积四锁（PASS）

⚠️ **方法学纠正**：交付说明中的 `node test/wiring-scan.js` **不可用于验收** —— 该文件是 `module.exports` 模块，直接执行**无任何输出且恒 `exit 0`**，无法证伪。QA 改为直接驱动 `scanSizes()` API。

```js
// node -e
const W=require("./test/wiring-scan.js");
const s=W.scanSizes(),B=W.SIZE_BUDGET;
```

```
=== 体积四锁 ===
contingency.js      4518 /     4973  余   455  PASS
engineNet           2100 /     2200  余   100  PASS
moduleSum          25803 /    28525  余  2722  PASS
total             273640 /   276480  余  2840  PASS

over = []
each = {"memory.js":13371,"presence.js":3557,"texture.js":4357,"contingency.js":4518}
```

逐字节核对（`wc -c` 独立复核）：

| 项 | 声称 | 实测 | 结论 |
|----|------|------|------|
| contingency.js | 4518 / 4973 | 4518 / 4973 | ✓ |
| engine.js | 247837 | 247837 | ✓ |
| engineNet | 2100 / 2200 | 2100 / 2200 | ✓ |
| moduleSum | 25803 / 28525 | 25803 / 28525 | ✓ |
| total | 273640 / 276480 | 273640 / 276480 | ✓ |
| contingency 相对 BASE 增量 | +432B | +432B（4086→4518） | ✓ |

### NOTE-1：engine 绝对上限口径不一致（18B）

```
engineBase 245737 + engineNetMax 2200 = 247937   ← 由 SIZE_BUDGET 推导
工程师声称上限                        = 247955   ← 宽 18B
真实余量 = 247937 - 247837 = 100B（非声称的 118B）
```

不影响本期判绿（两种口径下均通过），但**「V-33 余 118」这个数字是错的，真实余量 100B**。后续排预算请以 100B 为准，否则会超卖 18B。

---

## 四、R-C5 c4/c5（PASS）

### 4.1 c4/c5 实际可出场（独立探针，不复用工程师夹具）

```
[c4 情境·3000 次] 命中分布: {"sf":835,"c4":826} → c4 可出场: true
[c5 情境·3000 次] 命中分布: {"c5":841,"sf":828} → c5 可出场: true
c4 样例: 今天过得还不错哦，后来呢？我想听
c5 样例: 今天过得还不错哦，你说的火锅还顺利吗   （# 已回填为 value 原文）
```

### 4.2 `$` 回填硬化（+4B）确有其效

`.replace("#",()=>PW(F,r).value)` 的函数式 replacer 正确规避了 `$&` / `` $` `` / `$1` / `$$` 被当作替换模式：

```
PASS  value="$&火锅" → "今天过得还不错哦，我还记着$&火锅呢"
PASS  value="$`奶茶" → "今天过得还不错哦，你说的$`奶茶还顺利吗"
PASS  value="$1拉面" → "今天过得还不错哦，你说的$1拉面还顺利吗"
PASS  value="$$烧烤" → "今天过得还不错哦，我还记着$$烧烤呢"
```

### 4.3 CAP=2 独立于 recall 日配额（U-4 裁定成立）

```
同一 state 连打 300 轮命中次数 = 2 （CAP=2 期望恰为 2）→ PASS
改 d 为昨日后再打 300 轮       = 2 → 跨日重置 PASS
```

`AC-C5-1` 采用「每轮 fresh state + `st.ctg={}`」度量**候选池类型多样性**，`X-4` 单独钉死 CAP=2 本身，两者口径分离、互不放宽，符合 U-4。

### 4.4 变异测试：把选择器改回 `find`

**变异 A（语义变异，长度不等长）**：`PW(G.length?G:cd,r)` → `(G.length?G:cd).find(Boolean)`

```
not ok - V-113b · R-C5 到期声明：v15 已实装 c4/c5，且选择器已随机化
not ok - X-1   · 选择器已随机化：4 候选 + q.k 降权，200 次采样产出类型数 ≥3
not ok - AC-C5-6 · contingency.js 相对 BASE 增量 ≤470B…
# tests 308 / pass 301 / fail 3
```

**变异 B（等长变异，隔离体积干扰）**：`p=PW(G.length?G:cd,r);` → `p=(G.length?G:cd)[0] ;`（22→22 字符，文件恒 4518B）

```
X-1     → 红 ✓（语义承重）
V-113b  → 红 ✓（语义承重）
AC-C5-6 → 绿      ← 说明变异 A 中它转红只是体积假象
```

### NOTE-2：AC-C5-6 不是选择器的承重断言

交付说明称「改回 `find` 应导致 X-1/V-113b/**AC-C5-6** 打红」。等长变异证明 **AC-C5-6 转红仅因文件字节数变化**，它是体积钉，对选择器语义**零承重**。真正承重的只有 **X-1 与 V-113b（2 条）**。

### NOTE-3：选择器行号是 `:48` 不是 `:43`

```
48: if(!a&&cd.length&&E.chanceWith(.55,r)){const G=cd.filter(x=>x[0]!==q.k),p=PW(G.length?G:cd,r);k=p[0];a=p[1];}
```

`:43` 是 `sf` 兜底行。文档口径漂移，建议更正（不影响功能）。

---

## 五、T0 基线重置（PASS）

- `test/baseline.js:29` → `const BASE = "b86a386";` ✓（v14 收口）
- `PREV = BASE + "^"` ✓
- 已接入 6 个测试文件：`qa-v13-t2t4-fix` / `qa-v13-t5b` / `qa-v14-t2` / `qa-v14-t3` / `qa-v15-t1` / `qa-v15-t2`
- **S-4 纪律**：全 `test/` 无 `HEAD:ai-girlfriend/` 字面量残留（2 处命中均为说明该纪律的注释文本）

7 条自失效红逐条复核（按**实际验证**，采纳架构师口径）：

| 断言 | 状态 |
|------|------|
| `A1-a 配额数字落点` | ok 123 ✓（架构师指出其实为绿，实测确为绿，T1 抬配额后仍绿） |
| `A1-c engine.js 定点解冻白名单` | ok 125 ✓ |
| `C0-b sw.js CACHE 领先基线` | ok 152 ✓ |
| `T2 · V-92 覆盖矩阵 96/96` | ok 164 ✓ |
| `T2 · V-92 反证（旧表漏网 ≥60）` | ok 165 ✓ |
| `T2 · V-92 回归钉` | ok 167 ✓ |
| `T3 · V-102`（7 条） | ok 179–185 ✓（**真实第 7 红**，已修复） |

---

## 六、sw v20（PASS）

```js
const CACHE = "xiaonuan-v20";   // v20 v15 收线：…
```

`CACHE` 常量确为 **v20**；主理人 grep 先匹配到的 `v15` 出现在**同行注释**（“v20 v15 收线”）中，非常量值。
`C0-b`（ok 152）与 `AC-N2-5c · sw.js 缓存键已升 v20`（ok 255）均绿。

---

## 七、全量测试（PASS）

```
# tests 308
# suites 1
# pass 304
# fail 0
# todo 4
# duration_ms 9695.555114
```

284 → **308**（+24），**0 fail**。4 条 todo 均为显式挂账的历史缺陷（非本期回归）：

| todo | 内容 |
|------|------|
| R2-A5b | D4 六类缺「回避型」终止语 |
| R2-B4 | `app.js:2052` buildAffCurve 字典序排序 |
| Q-P2-D11 | `selfTick` 防重放可被改系统时间绕过 |
| （第 4 条） | 同批次挂账 |

**无过期快照导致的 fail，未发生断言翻转。**

---

## 八、变异测试 3/3（PASS）

```
[野生型] 破墙句被拦 8/8（期望全拦）
[野生型] 职业句被拦 0/5（期望 0 = 不误伤）

[M1 护栏失效]   破墙句被拦 0/8   → PASS 绿转红：H13 护栏确实承重
[M2 去折叠]     职业句被拦 5/5   → PASS 绿转红：R-P0 折叠确实承重
[M3 生成层注入] 泄漏 0 → 9       → PASS 绿转红：端到端探针确实能抓泄漏

综合: 全部断言非空有效
```

QA 追加的第 4 组变异（选择器 `find`，见 §4.4）亦为有效绿转红。

---

## 九、QA 新增资产

`test/qa-probe-v15-acceptance.js` —— 独立验收探针，三段式，可长期作回归闸：

| 段 | 内容 | 当前结果 |
|----|------|----------|
| P1 | 端到端 `E.reply()`：3 卡 × 2 性别 × 25 输入 × 3 turn = **600 行** | **PASS**（0 泄漏，≥480 达标） |
| P2 | 出口层：主理人指定 7 破墙 / 8 良性 | **FAIL**（漏 1） |
| P3 | 人称绑定泛化面 2100 组合穷举，**自动区分本期回归 / v14 既有** | **FAIL**（新回归 195） |

设计要点：破墙词表**独立自建**，不复用 `PERSONA_BREAK_RE`，避免「用被测者的尺子量被测者」；P3 内置 v14 基线正则做归因分流，使既有缺陷不会误伤本期判定。

---

## 十、遗留问题

### v15 内（必须本期修复，阻断收口）

| ID | 问题 | 位置 | 归属 |
|----|------|------|------|
| **Q-V15-1** | `我们都是模型训练的` 等 **195 条**人称+副词破墙句漏网（NOTE-2 引入的回归） | `engine.js:1307` | **工程师** |
| Q-V15-2 | 修复后需重置 6 条体积等值钉 + 体积评审 | `A4 / AC-N2-5 / T2 / T4 / V-108 / V-113` | **工程师 + 架构师** |
| Q-V15-3 | `qa-v15-t2.test.js` B1–B7 与 `AC-N2-1b` 未覆盖副词槽（照实现写用例） | `test/qa-v15-t2.test.js` | 工程师随源码修复同步补 |

### 非阻断 NOTE（本期可带走）

| ID | 问题 |
|----|------|
| N-1 | engine 绝对上限口径 247955 vs 推导 247937，**真实余量 100B 非 118B** |
| N-2 | `AC-C5-6` 对选择器语义零承重，交付说明将其列为承重断言不准确 |
| N-3 | 选择器实际在 `contingency.js:48`，文档写 `:43` |
| N-4 | `node test/wiring-scan.js` 无输出恒 exit 0，不能作为「over 为空」的证据 |

### 移交 v16

| ID | 问题 |
|----|------|
| V16-1 | 人称绑定组对 `算法/代码/bot/gpt/app/siri` 的副词槽同样只覆盖 4 词，**1170 条**组合漏网（v14 既有，非本期引入）。建议与 Q-V15-1 用同一套副词槽一次性收敛 |
| V16-2 | 4 条 todo 历史缺陷（R2-A5b / R2-B4 / Q-P2-D11 / 同批次第 4 条） |

---

## 十一、路由判定（第 1 轮 · 已执行完毕）

**Engineer**

- **源码 Bug**：`engine.js:1307` `PERSONA_BREAK_RE` 副词槽缺失 → 破墙护栏漏网（Q-V15-1），一票否决项
- **测试 Bug**：`qa-v15-t2.test.js` 用例集覆盖缺口（Q-V15-3）—— 与源码修复强耦合，**QA 不单独抢修**，避免与工程师改动冲突；QA 已落盘独立探针作为外部回归闸
- **验收态**：QA 全程只读验证，所有变异均已回滚，`engine.js` 247837B / `contingency.js` 4518B **逐字节复原**，全量 308/304/0/4 复跑确认无污染

> **QA 验收结论：FAIL / 不可收口。** 待 Q-V15-1 修复且体积钉重置后，QA 将进入第 2 轮回归（本轮为第 1 轮）。

---

# 十二、第 2 轮遗留问题与最终路由

## 12.1 第 1 轮阻断项处置结果

| ID | 问题 | 第 2 轮状态 |
|----|------|------------|
| **Q-V15-1** | 195 条人称+副词破墙句漏网 | **已闭合** ✓（7/7 拦、P3 新回归 0、M4 变异转红证明承重） |
| Q-V15-2 | 重置 6 条体积等值钉 + 体积评审 | **已闭合** ✓（engineNet 2160 已落 5 处；V-33 纠正为 247937；四锁 `over=[]`） |
| Q-V15-3 | B1–B7 与 AC-N2-1b 未覆盖副词槽 | **已闭合** ✓（B 表补至 B1–B12 含 B8；SHELLS 14→**37**；新增 U-5 定点守卫用例） |

## 12.2 第 1 轮 NOTE 处置

| ID | 第 2 轮状态 |
|----|------------|
| N-1 上限口径 247955 vs 247937 | **已修正** ✓ 改为 `SIZE_BUDGET.engineMax` 派生量并加载期自证；唯一保留的 247955 字面量在 A1-a 会计恒等式（v14 D-2 有意结构，已注释说明） |
| N-2 AC-C5-6 非承重 | 保留（文档措辞问题，不影响行为） |
| N-3 选择器行号 `:48` vs `:43` | **仍存在**（见 NOTE-A），纯文档漂移，不阻断 |
| N-4 `wiring-scan.js` 静默 exit 0 | **已确认并规避** ✓ 本轮全部体积结论直驱 `scanSizes()` |

## 12.3 移交 v16（不阻断本期收口）

| ID | 问题 |
|----|------|
| V16-1 | 人称绑定组对 `算法/代码/bot/gpt/app/siri` 的副词槽覆盖仍窄，**1170 条**组合漏网（v14 既有，非 v15 引入）。注：本轮副词槽扩容已顺带收敛一部分，建议 v16 一次性收敛剩余面 |
| V16-2 | 4 条 todo 历史缺陷：R2-A5b（回避型终止语召回）/ R2-B4（affHistory 字典序排序）/ Q-P2-D11（系统时间倒流泵送 self 四轴）/ A2-i（tag 应由 fact.key 派生） |
| V16-3 | **余量预警**：engineNet 仅余 **40B**（2160/2200），engine.js 同余 40B。v16 再动 `:1307` 极可能触锁，需提前立项 |
| V16-4 | contingency.js 已顶到 moduleSum 自洽天花板（U-7 存档），v16 若再动需破锁评审 |

## 12.4 最终路由判定

# **NoOne**

- **源码 Bug**：无。第 1 轮阻断项 Q-V15-1 已修复并经独立复验闭合
- **测试 Bug**：1 处，**QA 自身探针**的断言写法（文本启发式误报），已由 QA 自行修正，未路由工程师
- **验收态**：QA 全程只读验证。M4 物理变异已 **sha256 逐位校验还原**
  （`2d9fc6d6…a9ba`，247897B），还原后 `qa-v15-t2.test.js` 13/13 复绿，
  全量 **309/305/0/4** 复跑确认无污染；工作树无任何变异标记残留；
  `engine.js` 相对 HEAD 仍为**单行 diff**

> # QA 验收结论：PASS / 可收口
>
> v15 八项验收 **8/8 PASS**，一票否决项 H13 破墙密闭性全绿，
> 原阻断项 Q-V15-1 **已闭合**，变异测试 M1/M2/M3 + QA 追加 M4 **4/4 绿转红**（断言非空）。
> 按 2 轮硬上限纪律，**本轮为第 2 轮，验收到此收口**，无需第 3 轮。
> 遗留 V16-1~V16-4 均为非阻断项，随文档移交 v16。
