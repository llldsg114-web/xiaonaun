---
title: QA-ACCEPTANCE-v17 独立验收报告
version: v17
status: PASS（附 2 项已自修测试缺陷 + 3 项建议）
author: 严过关（QA 工程师）
upstream: PRD-v17.md（已批准）/ DESIGN-v17.md §2.5 §6 §7 §8
scope: R2-A5b 回避型终止语 · Q-P2-D11 selfTick 防重放 · R-S2 Tier3 二期 · 护栏归一化前置
---

# QA-ACCEPTANCE-v17 独立验收报告

## 0. 总判定

**VERDICT: PASS**

工程师交付声明（341/341 pass、over=[]、H13 0%、V33=248537、U-5=0、sw v22、contingency 5652≤5671）
经独立复核**全部属实**。所有数字均由 QA 自写探针 / 直驱函数实证，未采信交付报告。

验收期间发现 **2 个测试代码缺陷**（均为 v17 归一化重构导致的探针锚点陈旧，**非源码 Bug**），
已按智能路由规则由 QA 自行修复并复跑通过；**0 个源码 Bug**，无需回退工程师。

| 项 | 结论 |
|---|---|
| 源码 Bug | **0** |
| 测试代码 Bug | **2**（已自修） |
| 路由判定 | **NoOne** |
| 轮次 | 第 1 轮出关 |

---

## 1. H13 破墙密闭性（一票否决项）—— PASS，leak = 0

| 探针 | 口径 | 实测 | exit |
|---|---|---|---|
| `qa-probe-h13.js` | 3 人格 × 2 性别 × 20 输入 × 3turn = 480 行 | 泄漏 **0**，泄漏率 **0.000%** | 0 |
| `qa-probe-v15-acceptance.js` P1 | 端到端 `E.reply()` 600 行（要求 ≥480） | 泄漏 **0** | 0 |
| `qa-probe-v15-acceptance.js` P2 | NOTE-2 指定 7 破墙 / 8 良性 | 漏网 **0/7**、误杀 **0/8** | 0 |
| `qa-probe-v15-acceptance.js` P3 | **六维全组合 1,034,880** | 漏网 **0**（0.0000%） | 0 |
| `qa-v16-t1.test.js` | AC-G-1~9 十五条 | **15/15 pass** | 0 |
| `qa-rs2-type.test.js`（v17 新增） | AC-RS2-1~8 十七条 | **17/17 pass** | 0 |
| `qa-v16-independent-probe.js` | **独立异构网格 860,160**（与工程师网格无交集轴） | 漏网 **0** | 0 |
| `qa-r2-probe-h13-closure.js` | Q-V15-1 闭合 | PASS（已闭合） | 0 |

六维组合数算术自证：`7×2×22×4×8×15×7 = 1,034,880` ✓ 与口径一致。

**H13 破墙密闭性 = 0%，leak 恒 0，一票否决项通过。**

### 1.1 断言非空性证明（变异测试）

仅"测试全绿"不足以证明护栏承重，故复跑变异探针 `qa-probe-mutation.js`（**修复锚点后**）：

| 变异体 | 野生型 | 变异后 | 判定 |
|---|---|---|---|
| M1 `PERSONA_BREAK_RE` 置空 | 破墙 8/8 拦 | **0/8 拦** | PASS 绿转红 |
| M2 抹掉 pnorm 折叠段 | 职业句 0/5 误杀 | **5/5 误杀** | PASS 绿转红 |
| M3 生成层注入破墙文案 | 端到端泄漏 0 | **泄漏 8** | PASS 绿转红 |

⇒ H13 相关断言**确实承重、非空转**。

---

## 2. 体积四锁 —— PASS，over = []

⚠ **取证方法说明**：裸跑 `node test/wiring-scan.js` **无任何输出且恒 exit 0**（已实测复现），
**不可作为证据**。本项由 QA 自写 `test/qa-v17-independent-size.js` **直驱 `scanSizes()`** 取证。

⚠ **发现的口径盲区**：`scanSizes().over` **只检查 4 个模块**，不检查 engine.js / moduleSum / total
三把锁。故 `over=[]` 单独不足以证明四锁；QA 探针已对三者**分别硬断言**。

### 2.1 实测 vs 配额

| 项 | 实测 | 上限 | 余量 | 判定 |
|---|---|---|---|---|
| `engine.js` | **248353** | 248537 | 184 | ✓ |
| `engineNet` | **2616** | 2800 | 184 | ✓ |
| `memory.js` | 13333 | 13824 | 491 | ✓ |
| `presence.js` | 3566 | 3840 | 274 | ✓ |
| `texture.js` | 4366 | 4608 | 242 | ✓ |
| `contingency.js` | **5652** | 5671 | **19** | ✓（缓冲极薄，见 §6） |
| `moduleSum` | 26917 | 27943 | 1026 | ✓ |
| `total` | 275270 | 276480 | 1210 | ✓ |
| `over` | **[]** | — | — | ✓ |

### 2.2 四锁恒等式（DESIGN §2.5 唯一自洽解）

| 锁 | 表达式 | 验算 | 判定 |
|---|---|---|---|
| ① | `engineMax = engineBase + engineNetMax` | `248537 = 245737 + 2800` | ✓ |
| ② | `Σ(4 模块配额) = moduleSumMax` | `13824+3840+4608+5671 = 27943` | ✓ 严格等式 |
| ③ | `engineBase+engineNetMax+moduleSumMax = totalMax` | `276480 = 276480`（**松弛 0**） | ✓ |
| ④ | 各模块实测 < 各配额 | 无倒挂（见 §2.1） | ✓ |

预算表 9 个字段与 DESIGN §2.5 唯一解**逐字段对拍全等**（探针把真值写死在自身，避免"读预算表自证预算表"的循环论证）。
`Buffer.byteLength` 与 `statSync().size` 交叉复核 5 个文件**全部一致**（PRD §1 口径）。
`totalMax` 未被抬顶，270KB 承诺守住。

---

## 3. V33 三处同步 —— PASS，三处均 = 248537

DESIGN §7 枚举的**运行时**硬编码点共 3 处（#2/#4 为"仅引用不改值"，#6/#7 为归档文档）：

| # | 位置 | 实读 | 判定 |
|---|---|---|---|
| 1 | `test/wiring-scan.js:291` `SIZE_BUDGET.engineMax` | **248537** | ✓ |
| 3 | `test/qa-v13-t2t4-fix.test.js:117` `const V33` | **248537** | ✓ |
| 5 | `test/qa-v16-size-probe.js:74` `V33 === engineMax` 校验 | **248537**（动态对拍 + 显式排除旧值） | ✓ |

> 📌 **纠正主理人清单假设**：第三处并非 `qa-v12-gates.test.js`，而是 **`qa-v16-size-probe.js`**
> （DESIGN §7 #5）。`qa-v12-gates.test.js` 本轮虽有改动，但**不含** V33 硬编码点。

### 3.1 `248137` 残留全仓扫描

`test/` 下**无任何存活的硬断言仍钉 248137**。命中项分类：

- `wiring-scan.js:228/238/240/242/252/263`、`qa-v13-t2t4-fix.test.js:104/106/112/227/228/249/265`
  → **均在注释块内**（历史审批链 changelog），非可执行断言。
- `qa-v16-size-probe.js:76-77` → **反向断言** `V33 !== 247955 && V33 !== 248137`，
  即"必须已脱离旧值"，语义正确且**期望**出现该字面量。
- `qa-v13-t1.test.js:93/95` → 注释 + **测试名**（见 §5 陈旧标签核查）。

加载期自证守卫 `wiring-scan.js:302` 未抛错，证明 `engineMax` 派生关系未漂移。

---

## 4. U-5 守卫 / 归一化前置 / sw 版本 —— PASS

### 4.1 U-5 守卫
`grep -c "模型训练|" engine.js` = **0** ✓（裸词不得入表，避免"模型训练营"类良性句误杀）

### 4.2 pnorm 单一真源（v17 核心项）

| 核查点 | 实测 | 判定 |
|---|---|---|
| `pnorm` 定义处数（engine.js） | **1**（`:1310`） | ✓ 单一真源 |
| `pnorm` 定义处数（memory/presence/texture/contingency/app） | **0** | ✓ 无自造副本 |
| 导出 | `engine.js:3993` 导出 `pnorm` | ✓ |
| 模块消费形态 | memory×2 / presence×1 / texture×1 / contingency×1，**全部 `E.pnorm`** | ✓ |
| engine 侧消费 | `PERSONA_BREAK_RE.test(` 共 **7** 处，**裸判 0 处** | ✓ 全部经 pnorm |
| 归一化调用时序 | `:1322` 等处均为 `PERSONA_BREAK_RE.test(pnorm(x))`，**归一化在判定前** | ✓ |

**折叠不一致已收口**：全部 **12 个消费点**（engine 7 + 模块 5）统一走 `E.pnorm`，
不再存在"texture.js:59 与 memory.js:107 完全不折叠、另 3 处各搞一套"的分歧（PRD P2-2 达成）。

### 4.3 PERSONA_BREAK_RE 四轴形态（`:1307`）
四轴结构逐位在位：人称 `[你我咱它他她您]们?` / 副词槽（含 `说?到底|究竟|根本|压根|难道|岂不`）/
`[都也还只就]{0,2}` / 系词 `(?:[是算当]|作为)` + 核心词组 + 职业后缀否定前瞻。
前瞻为 `(?!.{0,3}(?:方向|专业|工程师|研究生|审查员))` —— **含完整词「研究生」，无单字「研究」前瞻漂移** ✓
（独立探针 B''/B'''' 双证：前瞻项全部为多字完整词，无单字过贪。）
`:1307` 与 v16 `b57dd9f` **逐字节相同**，DESIGN §8 "v17 :1307 一字未动"属实 ✓

### 4.4 sw 缓存换键
`xiaonuan-v21` → **`xiaonuan-v22`** ✓
五个被缓存文件 `engine.js / contingency.js / memory.js / presence.js / texture.js`
经 `git diff HEAD` 核实**全部真实变更** ⇒ 换键必要且已执行，不会出现 v13 C0-b 式新旧混装。

---

## 5. 误杀面 / 全量回归 / 专项核查

### 5.1 变异·良性·职业 0 误杀 —— PASS

| 项 | 口径 | 实测 |
|---|---|---|
| v15 八良性句 | AC-G-3 | **0/8 误杀** |
| 职业/领域良性集 | AC-G-4（含 PRD §1.4 点名 10 条） | **14 条 0 误杀** |
| 真破墙句 | AC-G-4b（含前瞻过贪哨兵 4 条） | **0 漏网** |
| 六维全组合 | AC-G-1 | 1,034,880 → **0 漏网** |
| v15 P3 网格 | AC-G-2 | 2,100 → **0 漏网** |
| 独立异构网格 | QA 侧异构轴 | 860,160 → **0 漏网** |
| 双通道一致 | AC-G-6 裸正则 vs `guardPersonaReplies()` | **逐条相同** |

### 5.2 全量回归 —— PASS

```
node --test test/*.test.js
# tests 341 | pass 341 | fail 0 | cancelled 0 | skipped 0 | todo 0
```
**与交付声明 341/341/0/0 完全对齐** ✓

⚠ **过程缺口（非源码问题，须知悉）**：`package.json` 的 `test` 脚本 glob 为 `test/*.test.js`，
**不覆盖** 8 个 `qa-probe-*.js` / `qa-*-probe.js` 探针文件。这正是 §7-A 缺陷未被交付前发现的原因。
QA 已逐个手工执行，8/8 exit 0：

| 探针 | 结论 |
|---|---|
| `qa-probe-h13.js` | PASS（0 泄漏） |
| `qa-probe-mutation.js` | PASS（M1/M2/M3 全绿转红）※修复后 |
| `qa-probe-v15-acceptance.js` | PASS（六维 0 漏网） |
| `qa-r2-probe-h13-closure.js` | PASS（已闭合） |
| `qa-r2-probe-rc5.js` | PASS |
| `qa-v16-independent-probe.js` | PASS（异构网格 0 漏网） |
| `qa-v16-size-probe.js` | PASS（体积四锁） |
| `qa-v17-independent-size.js`（QA 新增） | PASS |
| `qa-v17-adversarial.js`（QA 新增） | PASS（1 WARN） |

### 5.3 ⚠ 陈旧标签核查 `qa-v13-t1.test.js:95` —— cosmetic，**非 FAIL**

测试**名**写「≤248137B」（v16 旧值），但实际断言体：

```js
const CAP = W.SIZE_BUDGET.engineMax, left = CAP - size;   // :97 动态取值
assert.ok(size <= CAP, ...);                              // :99
```

**结论：读 `SIZE_BUDGET.engineMax`（动态），非硬编码。**

**决定性反证（自然实验）**：当前 `engine.js = 248353B > 248137B`。
若断言真为硬编码 248137，该用例**必然 FAIL**；实测运行输出
`[V-33] engine.js = 248353B / 上限 248537B，剩余 184B` 且 `ok 3` 通过
⇒ 上限确为 248537（动态），**断言有效、非形同虚设**。

判定：**cosmetic 陈旧标签**，记录并建议工程师改标签，不阻断验收。

**追加发现（主理人清单未覆盖）**：同文件 `:102` 测试名写「V-90：engine 净增 ≤ 2048B」，
实际 CAP 亦走 `SIZE_BUDGET.engineNetMax` = 2800（实测 net 2616 > 2048，同理反证为动态）。
**两条陈旧标签应一并修正。**

### 5.4 baseline.js 差分基线 —— 与主理人清单预期不同，但**符合已批准 DESIGN**

实测：`BASE = "b86a386"`（**v14** 收口）、`PREV = BASE + "^"` = `eb21332`（v13），
`baseline.js` 本轮 **未被修改**（`git diff HEAD` 无该文件）。

主理人清单第 10 项预期"T0 已将 BASE/PREV 前移至 v16 `b57dd9f` 及其父"，**实际未前移**。
但查 **DESIGN-v17 §8**（标题即《确认 `baseline.js` 无需 reset》）已正式裁定：

> **裁定：v17 不 reset `BASE`**。…… 3. 若误 reset 到 v16 `b57dd9f`，会连带要求重审
> v15/v16 所有 A1-c 记录，**引入无收益风险**。

且 `test/wiring-scan.js` §7 清单第 580 行明确标注 `test/baseline.js | BASE | **不改**`。
⇒ 工程师**遵循了已批准设计**，非漏做。

**差分断言是否自失效？—— 否，已实证**：
- `qa-v15-t1.test.js:399` 以 `assert.strictEqual(base, 4086)` **钉死 v14 基线文件内容**，
  基线一旦漂移立即红，不存在静默失效。
- `:425-428` 对 memory/presence/texture 以 **`strictEqual` 钉净字节精确值**
  （−38 / +9 / +9），比"有无 diff"更严，且当前全绿。
- A1-c 白名单：`:1307` 相对 v16 **逐字节未变**，白名单 `[1307]` 成立。

判定：**PASS**（但存在文档内部矛盾，见 §7-C）。

---

## 6. QA 自写独立探针（新增 2 个）

既有测试全部出自工程师之手，QA 另写两个探针独立取证：

### 6.1 `test/qa-v17-independent-size.js`
把 DESIGN §2.5 唯一解**写死在探针内部**再与 `SIZE_BUDGET` 对拍，规避"读预算表自证预算表"；
补齐 `scanSizes().over` 不覆盖的 engine / moduleSum / total 三把锁。结论 PASS。

### 6.2 `test/qa-v17-adversarial.js`（对抗探针，专打 v17 新面）
既有测试只证"护栏还在拦"，**未攻击归一化层本身，也未证各消费点口径一致**。本探针补此二者：

| 段 | 内容 | 结论 |
|---|---|---|
| A | 归一化闸效力：全角 `ＡＩ` / 空格切分 `我 是 A I` / 制表符 / 全角空格 4 类变体 × 4 破墙句 = 20 组，**必须与裸形态同判** | **20/20 同判命中** ✓ |
| B | 跨消费点一致性：模块侧 5 处 + engine 侧 7 处，**全部经 `E.pnorm`，裸判 0** ；运行时 7 串对拍 engine 出口 vs 裸正则 | **12/12 收口、7/7 同判** ✓ |
| C | 折叠一致性：程序族三变体 → 同一 token「职」，且**幂等** | ✓ |
| D | 零宽字符边界 | **WARN**（见 §7-D） |

⇒ v17「护栏归一化前置 + pnorm 统一收口」主张**成立且可复现**。

---

## 7. 发现的问题清单

### A. 【已自修 · 测试代码 Bug】`qa-probe-mutation.js` 变异锚点陈旧导致**探针崩溃**

- **现象**：`node test/qa-probe-mutation.js` 抛 `Error: 变异锚点失效: :1322 等长折叠`，进程崩溃退出。
- **根因**：v17 S-1b 把等长折叠从 `:1322` 内联式 `probe.replace(/程序[员猿媛]/g,"职")`
  **上收进 `:1310` 的 `pnorm`**，M2/M3 两处锚点字符串随之失配。**源码正确，探针陈旧。**
- **严重性**：**中高**。该探针是证明 H13 断言"非空转"的唯一手段，崩溃期间 M2/M3 从未执行；
  且因不在 `npm test` glob 内，交付自检不会暴露。
- **路由**：测试代码 Bug → **QA 自行修复**（符合规则）。
- **修复**：M2 锚点改钉 `pnorm` 内折叠段 `.replace(/程序[员猿媛]/g,"职")`；
  M3 返回行锚点改为 `return PERSONA_BREAK_RE.test(pnorm(probe)) ? PERSONA_FALLBACK : fixed;`。
- **复跑**：M1/M2/M3 **全部 PASS 绿转红**（M2 去折叠后职业句 5/5 误杀，证明折叠承重）。

### B. 【已自修 · QA 自身测试 Bug】`qa-v17-adversarial.js` C 段断言表达式错误
QA 首版误以"折叠等长"为安全属性并写出病态表达式，实际 `程序员(3)→职(1)` 为**有意的非等长**归约，
正则不依赖字符偏移故无害。已改为断言**三变体折叠一致性 + 幂等性**，复跑全绿。
（记录在案以示 QA 自身产物同样受审。）

### C. 【建议工程师/架构师 · 文档不一致】DESIGN-v17 §0 与 §8 自相矛盾
- §0 T0 行（`:23`）写：`四锁翻转 + V33 同步 + **差分基线前移**`，涉及文件列 `baseline.js`；
- §8（`:668`）却裁定：**「v17 不 reset BASE」**，§7 清单 `:580` 亦写 `baseline.js | BASE | 不改`。

工程师按 §8 执行（正确），但 §0 表述会误导后续验收者判为"漏做 T0"（本次即触发该误判）。
**建议**：将 §0 T0 行"差分基线前移"改为"差分基线**复核（裁定不 reset）**"，并从文件列移除 `baseline.js`。

### D. 【风险登记 · 不阻断】零宽字符可绕过 `pnorm` 归一化
`pnorm` = `NFKC` + `\s+` 剥离 + 程序族折叠。NFKC **不清除**零宽字符，JS `\s` 亦**不匹配** `U+200B/200C/200D`。
实测 `我是A\u200BI` **不被拦截**。
- **现实风险：低**。H13 威胁模型是"AI 自身输出泄漏"，生成侧为固定语料库，不会自发插入零宽字符。
- **建议 v18**：`pnorm` 增补 `.replace(/[\u200B-\u200D\uFEFF]/g,"")`（约 +30B），纳入 engineNet 预算。

### E. 【建议工程师 · cosmetic】两条陈旧测试标签
- `qa-v13-t1.test.js:95` 名称「≤248137B」→ 应为 `≤248537B`（断言体动态，无功能问题）
- `qa-v13-t1.test.js:102` 名称「≤ 2048B」→ 应为 `≤ 2800B`（同上）

### F. 【建议 · 过程】`npm test` glob 不覆盖探针文件
`"test": "node --test test/*.test.js"` 漏掉 8 个探针。**建议**追加脚本：
`"test:probe": "for f in test/qa-*probe*.js test/qa-v17-*.js; do node $f || exit 1; done"`，
纳入交付自检，避免 §7-A 类缺陷再次潜伏。

---

## 8. 遗留 / 风险提示

| 项 | 内容 | 等级 |
|---|---|---|
| R-1 | `contingency.js` 余量仅 **19B**（5652/5671）。R-S2 语料再加任何一条即破锁 ②/④。后续须走 DESIGN §9-Q2 降级（砍语料 3→2 条），**不得**申请二次配额 | **高** |
| R-2 | `engine.js` / `engineNet` 余量 **184B**，`:1307` 任何扩展须先走预算评审（PRD P0-1） | 中 |
| R-3 | 三锁松弛为 **0**，`totalMax` 打满即恰好；任何一侧超支都会直接击穿天花板，无缓冲 | 中 |
| R-4 | 零宽字符归一化盲区（§7-D） | 低 |

---

## 9. 结论

**IS_PASS: YES** · **路由：NoOne**（0 源码 Bug，2 个测试缺陷已由 QA 自修并复跑通过）

v17 四项交付 —— R2-A5b 回避型终止语、Q-P2-D11 selfTick 防重放、R-S2 Tier3 二期（落 contingency 5652B）、
护栏归一化前置（pnorm 统一收口 12 个消费点）—— 均已独立实证达标。
体积路径 A（Δ=+400，engineNetMax 2400→2800，totalMax 不动）四锁自洽、松弛 0、over=[]。
H13 破墙密闭性 **0%**，六维 1,034,880 + 异构 860,160 双网格 leak 恒 **0**，一票否决项通过。

建议工程师处理 §7-C（文档矛盾）、§7-E（陈旧标签）、§7-F（glob 缺口）三项非阻断改进，
并在 v18 评估 §7-D 零宽字符加固。
