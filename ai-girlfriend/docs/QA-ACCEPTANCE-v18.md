# QA 独立验收报告 · v18

- **验收人**：严过关（QA 工程师）
- **对象**：AI 女友 v18（contingency 配额重分配 A2 + 零宽字符加固 + test:probe 入网）
- **基线提交**：`47c35c6`（v17 收线）
- **验收纪律**：不采信工程师交付声明与其自写测试结论，全部结论由 QA 自写/自驱探针实证
- **轮次**：第 1 轮（自修 1 处 QA 侧测试 bug，无源码 Bug）

## 总判定

> **PASS** — 10 项验收清单全部实证通过，路由判定 **NoOne**（无需工程师返工）。

| # | 验收项 | 结论 | 关键实测值 |
|---|--------|------|-----------|
| 1 | H13 破墙密闭性 0%（一票否决） | ✅ PASS | 五套探针全 exit0，leak 恒 0 |
| 2 | 零宽加固绿转红取证 | ✅ PASS | SPLIT 8/8 v17 漏 → v18 拦 |
| 3 | 体积四锁 `over=[]` | ✅ PASS | 直驱 `scanSizes()`，四锁均有余量 |
| 4 | V33 三处 = 248537 | ✅ PASS | 三处逐一实读一致 |
| 5 | U-5 守卫 | ✅ PASS | `grep -c "模型训练\|"` = 0 |
| 6 | 四模块源码零变化 | ✅ PASS | `git diff --numstat` 空输出 |
| 7 | sw 版本 v23 | ✅ PASS | `xiaonuan-v23`，换键依据成立 |
| 8 | test:probe 可跑 | ✅ PASS | 全量/快子集均 exit0 |
| 9 | 良性/职业 0 误杀 | ✅ PASS | 844 组合 0 误杀 |
| 10 | 全量回归 | ✅ PASS | **349/349 pass / 0 fail / 0 todo** |

---

## 1 · H13 破墙密闭性（一票否决项）

五套探针独立执行，**exit code 逐一捕获**（注：`PIPESTATUS` 在 zsh 下不可用，改用直捕）：

| 探针 | exit | 结论 |
|------|------|------|
| `test/qa-probe-h13.js` | 0 | 泄漏率 **0.000%** |
| `test/qa-probe-v15-acceptance.js` | 0 | P1/P2/P3 全 PASS |
| `test/qa-v16-t1.test.js` | 0 | 15/15 pass |
| `test/qa-v17-adversarial.js` | 0 | FAIL 0 / WARN 0 |
| `test/qa-v18-zerowidth.test.js` | 0 | 8/8 pass |

**六维全组合闭环**（`qa-probe-v15-acceptance.js` P3）：

```
维度: 人称7 × 们2 × 副词22 × 系词4 × 量词8 × 核心15 × 尾缀7
组合总数 1,034,880（口径 1,034,880）/ 漏网 0（漏网率 0.0000%）
```

破墙漏网 **0/7**、良性误杀 **0/8**。**leak === 0，一票否决项通过。**

---

## 2 · 零宽字符加固：绿转红取证（本轮核心）

### 2.1 QA 取证手法强于工程师自写测试

工程师测试 `qa-v18-zerowidth.test.js` 的对照组 `pnormV17` 是**手写复述**的 v17 三段形态。
QA 不接受复述 —— 改用**从 git 检出真实 v17 引擎**并排跑：

```bash
git show 47c35c6:./engine.js  > /tmp/v17ref/engine.js   # 248353B
git show 47c35c6:./{memory,presence,texture,contingency}.js
```

自写探针 `test/qa-v18-edward-probe.js` 同时加载 v17 / v18 两套引擎，
经**出口闸 `guardPersonaReplies()` 端到端**判定（非仅比对 `pnorm` 字符串）。

先验证复述保真：v17 真实 `:1310` 与测试内 `pnormV17` **逐段一致**，对照组无循环论证。

```
v17: normalize("NFKC").replace(/\s+/g,"").replace(/程序[员猿媛]/g,"职")
v18: normalize("NFKC").replace(/[\u200B\u200C\u200D\uFEFF]/g,"").replace(/\s+/g,"").replace(/程序[员猿媛]/g,"职")
```

### 2.2 SPLIT 组绿转红 —— **坐实**

样例须真正「切开破墙词**本体**」（非词间插入）。8 条全部满足：

| 样例 | 切开对象 | v17 | v18 |
|------|---------|-----|-----|
| `我是A\u200BI` | AI | **漏(红)** | 拦(绿) |
| `你是个机\u200C器人` | 机器人 | **漏(红)** | 拦(绿) |
| `我是个程\u200D序` | 程序 | **漏(红)** | 拦(绿) |
| `我是语言模\u200B型` | 语言模型/模型 | **漏(红)** | 拦(绿) |
| `机器\u200B人`（主理人指定） | 机器人 | **漏(红)** | 拦(绿) |
| `程\u200D序`（主理人指定） | 程序 | **漏(红)** | 拦(绿) |
| `你是聊天机\u200B器人吧` | 机器人（长句） | **漏(红)** | 拦(绿) |
| `我是人工智\u200C能` | 人工智能 | **漏(红)** | 拦(绿) |

> **v17 漏网 8/8 → v18 拦截 8/8。绿转红成立，断言非空转。**
> 主理人点名的 `机器\u200B人` / `程\u200D序` 两条均在组内且均坐实。

### 2.3 工程师自曝的 DESIGN §3.2.4 A 段勘误 —— **属实**

QA 独立复核 A 段原列 4 条样例在**真实 v17 引擎**下的表现：

| 样例 | v17 实测 | 是否构成绕过证据 |
|------|---------|----------------|
| `我是A\u200BI` | 漏 | ✅ 是 |
| `你是个\u200C机器人` | **本就拦** | ❌ 否（零宽落在词外，「机器人」仍连续） |
| `我\u200D只是个程序` | **本就拦** | ❌ 否（「程序」仍连续） |
| `\uFEFF我是语言模型` | **本就拦** | ❌ 否（`\s` 已覆盖 U+FEFF） |

**A 段 4 条中仅 1 条在 v17 真漏（1/4）** —— 与工程师自曝完全一致，属实。
工程师另立 SPLIT 组取证的处置**正确**，且 SPLIT 组确为「切开词本体」，非沿用错误样例。
A 段 4 条在 v18 下亦全部被拦（不回归）。

### 2.4 QA 侧发现：黑名单四字符中仅 3 个承重

`/\s/` 对四个码点的覆盖实测：

```
U+200B  /\s/.test = false     ← v17 真实绕过通道
U+200C  /\s/.test = false     ← v17 真实绕过通道
U+200D  /\s/.test = false     ← v17 真实绕过通道
U+FEFF  /\s/.test = true      ← v17 已能处理（ECMA-262 WhiteSpace 历史特例）
```

⇒ `U+FEFF` 在黑名单中属**冗余显式项**，不新增拦截能力。
**判定：非缺陷。** 符合 DESIGN §3.2.2「枚举式防范围蔓延」的设计意图，属防御纵深，
且 `U+FEFF` 显式列出可在未来 `\s` 语义变动时保持稳定。已补测 1b 段断言其「不回归」。

### 2.5 穷举与副作用

- **破墙侧**：8 破墙句 × 4 零宽 × 全插入位 = **216 组合，漏网 0**
- **emoji ZWJ（N-1）**：`👨‍👩‍👧` / `👩‍💻` / `🏳️‍🌈` 经输出路径**逐字节不变**，ZWJ 保留；
  反向取证 `pnorm("👨\u200D👩") === "👨👩"` 证明判定副本内确被剥离（非「碰巧没坏」）
- **幂等**：9 例 `pnorm(pnorm(x)) === pnorm(x)` 全成立

---

## 3 · 体积四锁（直驱 `scanSizes()`）

裸跑 `wiring-scan.js` 恒 exit0 且无输出，**不可作为证据**。QA 以 `require()` 直驱：

```js
const W = require("./test/wiring-scan.js");
const B = W.SIZE_BUDGET, s = W.scanSizes();
```

### 3.1 实测输出

```json
{
  "each": { "memory.js": 13333, "presence.js": 3566,
            "texture.js": 4366, "contingency.js": 5652 },
  "engine": 248395, "engineNet": 2658,
  "moduleSum": 26917, "total": 275312,
  "over": []
}
```

### 3.2 四锁余量

| 锁 | 实测 | 上限 | 余量 |
|----|------|------|------|
| engine | 248395 | 248537 | **142** |
| engineNet | **2658** | 2800 | **142** |
| moduleSum | 26917 | 27943 | 1026 |
| total | 275312 | 276480 | 1168 |

> **`over = []`** ✅　engineNet 实测 2658，与预期一致。

### 3.3 三条恒等式

| # | 恒等式 | 实测 | 结论 |
|---|--------|------|------|
| ① | `engineMax === engineBase + engineNetMax` | `248537 === 245737 + 2800` | ✅ |
| ② | `Σ四配额 === moduleSumMax` | `13365+3598+4398+6582 = 27943 === 27943` | ✅ |
| ③ | `base + net + moduleSum === totalMax` | `245737+2800+27943 = 276480 === 276480` | ✅（松弛 0） |

恒等式 ① 亦由 `wiring-scan.js` **加载期自证**（失配即 throw），双重保障。

### 3.4 配额落位与顶层冻结

四模块配额：`memory 13365 / presence 3598 / texture 4398 / contingency 6582` —— **逐项一致** ✅

顶层三值 + 两红线**一个字节未动**：
`engineBase 245737` / `engineNetMax 2800` / `engineMax 248537` / `moduleSumMax 27943` / `totalMax 276480` ✅

⇒ A2「纯模块侧内部重分配」路径成立。

---

## 4 · V33 三处 = 248537

| # | 位置 | 形态 | 实读值 |
|---|------|------|--------|
| 1 | `test/wiring-scan.js:328` | `engineMax: 248537,` | **248537** ✅ |
| 2 | `test/qa-v13-t2t4-fix.test.js:117` | `const V33 = 248537;` | **248537** ✅ |
| 3 | `test/qa-v16-size-probe.js:74` | `Number(mV33[1]) === B.engineMax` | **248537** ✅（动态取值；`:47` 另有硬断言 `248537`） |

行号 291→328 的漂移由 `wiring-scan.js` 新增审批链注释块（+43/−6 行）造成，**值未变**，与声明一致。

### 4.1 `248137` 残留全仓扫描

`test/` 下**无任何存活的硬钉**。命中项分类：

- `qa-v16-size-probe.js:76-77` → **反向断言** `V33 !== 247955 && V33 !== 248137`，语义正确，须保留
- `wiring-scan.js:228/238/240/242/252/263`、`qa-v13-t2t4-fix.test.js:104/106/112/227/228/249/265`
  → 全在**注释块**内（历史沿革记录），非运行时
- `docs/` 下命中 → 历史文档，不在运行时

⇒ **无存活硬钉** ✅

---

## 5 · U-5 守卫

```
grep -c "模型训练|" engine.js  →  0
```

✅ 通过。

---

## 6 · 四模块源码零变化

```
git diff --numstat 47c35c6 -- memory.js presence.js texture.js contingency.js
（空输出）
```

字节数复核：`memory 13333 / presence 3566 / texture 4366 / contingency 5652` —— 与 v17 **逐字节一致** ✅

### 6.1 全改动面审计（相对 47c35c6）

非测试、非文档的源码改动**仅 3 个文件**：

| 文件 | +/− | 内容 |
|------|-----|------|
| `engine.js` | 1/1 | `:1310` pnorm 追加 seg2（**唯一功能改动**） |
| `package.json` | 4/1 | `test:probe` / `test:probe:fast` 脚本 |
| `sw.js` | 1/1 | 缓存键 v22→v23 |

`engine.js` 实际 diff 仅 **2 行**（1 增 1 删，同一行改写）。
字节 248353 → 248395，**Δ = +42B**，与 `.replace(/[\u200B\u200C\u200D\uFEFF]/g,"")` 长度精确吻合。
行数 **4001 → 4001 不变**，「行数不变铁律」成立（`:1310` 仍为 pnorm 定义行，G1 断言实证）。

---

## 7 · sw 版本 v23

```
grep -o "xiaonuan-v[0-9]*" sw.js  →  xiaonuan-v23
```

换键依据（C0-b 纪律）实证：

```
engine.js  v17 sha256: c402be90d0acb81a…
engine.js  v18 sha256: 829acc93302f1823…   ← 内容确变
```

被缓存文件 `engine.js` 内容已变 ⇒ **必须换键**，否则老用户命中旧缓存等于加固未上线
（v13 C0-b 事故成因）。v22→v23 ✅ 正确。

---

## 8 · test:probe 脚本真实可跑

| 命令 | exit | 覆盖 |
|------|------|------|
| `npm run test:probe` | **0** | mutation / v17-adversarial / v17-independent-size / probe-h13 / v15-acceptance（5 套） |
| `npm run test:probe:fast` | **0** | probe-h13 + v17-adversarial（**恰为 2 套**）✅ |

快子集内容与声明一致。变异探针 `qa-probe-mutation.js` M1/M2/M3 **三段绿转红全 PASS**，
证明 H13 护栏与 R-P0 折叠**确实承重**，断言非空转。

---

## 9 · 良性 / 职业 0 误杀

| 集合 | 条数 | 裸形态误杀 | 零宽×全插入位误杀 |
|------|------|-----------|-----------------|
| 八良性（高达模型系列） | 8 | **0/8** | 0 |
| 职业句 | 14 | **0/14** | 0 |
| U-5 保护句（训练系列） | 4 | **0/4** | 0 |
| **合计** | 26 | **0** | **0 / 844 组合** |

基线非空性已先行验证（裸形态若误杀则基线已坏）。✅

---

## 10 · 全量回归

```
npm test  →  exit 0

# tests 349
# suites 1
# pass 349
# fail 0
# cancelled 0
# skipped 0
# todo 0
# duration_ms 5624.9
```

**349 / 349 pass / 0 fail / 0 todo** —— 与工程师声明**完全对齐** ✅
全量输出 `not ok` 计数 = **0**。

新增 `qa-v18-zerowidth.test.js` 确已入网（tests **290–297**，8 段全绿）：

```
ok 290 - AC-ZW-A · 零宽绕过样例全部被归一化消灭，且可证 v17 形态确实漏网
ok 291 - AC-ZW-B · pnorm 幂等
ok 292 - AC-ZW-C · 零宽变体与去零宽原句逐字节相同
ok 293 - AC-ZW-D · 含 ZWJ 的 emoji 输出路径逐字节不变（N-1）
ok 294 - AC-ZW-E · 八良性 + 14 职业 + U-5 插 ZWSP 后 0 误杀
ok 295 - AC-ZW-F · 全仓语料静态扫描无真实零宽码点
ok 296 - AC-ZW-G1 · S-1b 单一真源
ok 297 - AC-ZW-G2 · N-1 形态钉（消费点恒 12）
```

> 注：工程师声明为「新增 1 个测试文件」，实际贡献 **8 个用例**；349 总数已含之。

---

## 11 · QA 侧自修记录（测试代码 Bug，非源码 Bug）

第 1 轮 QA 自写探针 `qa-v18-edward-probe.js` 初版报 **2 FAIL**，经归因为 **QA 自身测试 bug**，
按路由规则「测试代码 Bug → 自行修复」处置，**未反馈工程师**：

| 项 | 初版错误预期 | 实际 | 归因 |
|----|------------|------|------|
| `我是人工智\uFEFF能` | 预期 v17 漏（绿转红） | v17 本就拦 | `\s` **匹配** U+FEFF，v17 三段已处理 |
| SPLIT 组汇总断言 | 连带失败 | — | 同上 |

**修复**：将 U+FEFF 从绿转红组移出，另立 `1b` 段断言「v17/v18 均拦（不回归）」；
新增 `0` 段前置事实断言，显式钉住 `\s` 对四码点的覆盖差异。
修复后探针 **FAIL 0 / exit 0**。

⚠️ 该 bug 不影响任何源码结论 —— 工程师测试文件头部注释（`qa-v18-zerowidth.test.js:8-9`）
**已正确记载** 此特例，说明工程师对该机制理解无误。

### 11.1 QA 新增产物

- `test/qa-v18-edward-probe.js` —— QA 独立验收探针（双引擎并排绿转红取证）。
  未接入 `package.json`（脚本归工程师域）；如需常驻，建议后续并入 `test:probe`。

---

## 12 · 路由判定

| 判定 | 结果 |
|------|------|
| 源码 Bug | **无** |
| 测试代码 Bug | 1 处（QA 自身探针），**已自修** |
| **路由** | **NoOne** |

**无需工程师（寇豆码）返工。** 第 1 轮即通过，未进入第 2 轮。

## 13 · 遗留与建议（不阻断发布）

1. **`U+FEFF` 冗余项**（§2.4）：黑名单四字符中仅 3 个承重。非缺陷，建议在 DESIGN §3.2.2
   补一句「U+FEFF 与 `\s` 覆盖重叠，显式列出为防御纵深」，避免后人误删。
2. **四模块缓冲极紧**：memory/presence/texture 各仅余 **32B**，contingency 余 930B。
   v19 任何模块侧增量须**先重谈配额**，不可直接写码。
3. **`qa-v13-t1.test.js:95`** 测试名仍写「≤248137B」（PRD-v18 P1-3 列为 cosmetic 顺延项）。
   断言体动态取 `SIZE_BUDGET.engineMax`，**无功能问题**；实测 engine 248395 > 248137 而该用例
   仍绿，反证其确为动态取值。建议 v19 随手修名。
4. **`engineNet` 余量 142B**：engine 侧与 V33 两把锁**重合**（设计性间隙 0），
   v19 若动 engine.js 需同步评估 `engineNetMax`。

---

*报告完 · 严过关 · v18 独立验收 · 第 1 轮 PASS*

